apiVersion: v1
kind: Service
metadata:
    name: bookcatalog-postgres
spec:
    selector:
        app: bookcatalog-postgres
    ports:
        - port: 5432
          targetPort: 5432
    type: ClusterIP
